[pytest]
testpaths = tests
pythonpath = .
addopts =
    --cov=game
    --cov-report=term-missing
    --cov-report=html
    --cov-branch
    --cov-fail-under=70
markers =
    core: basic command functionality
    fs: filesystem mutation tests
    integration: pipes, redirects, multi-command flows
    slow: expensive scenario tests